# Model specific directories
dir_registry = {
    "reconstruction_dir" : "reconstruction", # Folder to store full reconstructed signal for brief validation of model
    "model_params_dir" : "models", # Folder to store the serialized model parameters for each trained model
    "results_dir" : "results", # Folder to store the results (data and plots)
    "raw_analysis_data_dir" : "raw" # Folder to store serialized model analysis results
}